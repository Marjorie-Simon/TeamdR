<p class="date text-center" style="position: absolute;"><%= Date.today.strftime("%A, %d %b %Y") %></p>

<div class="menu-left menu col-sm-3 col-lg-2">
  <ul>
    <li>
      <%= link_to "My Calendar", root_path, class:"menu-item #{"active" if current_page?(root_path)}" =%>
    </li>
    <li>
      <%= link_to "Colleagues", users_path, class:"menu-item #{"active" if current_page?(users_path)}" =%>
    </li>
    <%# <li>
      <a href="#" class="menu-item"> Teams </a>
    </li> %>
    <li>
      <%= link_to "Events", events_path, class:"menu-item #{"active" if current_page?(events_path)}" =%>
    </li>
    <% if user_signed_in? && current_user.manager %>
      <li>
      <%= link_to "Dashboard", dashboard_path, class:"menu-item #{"active" if current_page?(dashboard_path)}" =%>
      </li>
    <% end %>
  </ul>
  <% if user_signed_in? %>
    <% unless current_user.todaysgoal.empty? %>
  <div class="card-goal">
    <div>
      <h5>Today I will:</h5>
      <h6><%= current_user.todaysgoal %></h6>
    </div>
    <div>
      <p>Let's make it happen 🏋️‍♀️!</p>
    </div>
  </div>
  <% end %>
  <% end %>
</div>
